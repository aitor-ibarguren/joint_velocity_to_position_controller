name: ROS2 Jazzy CI

on:
  push:
  pull_request:

jobs:
  build-and-test:
    runs-on: ubuntu-24.04

    steps:
      # 1. Set up ROS 2 Jazzy
      - name: Set up ROS 2 Jazzy
        uses: ros-tooling/setup-ros@v0.7
        with:
          required-ros-distributions: jazzy

      # 2. Create workspace & copy files from the clone repo
      - name: Create workspace & copy files
        run: |
          cd ..
          mkdir -p ros_ws/src
          cp -r $GITHUB_WORKSPACE ros_ws/src

      # 3. Install dependencies with rosdep
      - name: Install dependencies
        run: |
          cd ../ros_ws
          rosdep update
          rosdep install --from-paths src -y --ignore-src --rosdistro jazzy

      # 4. Build with colcon
      - name: Build with colcon
        run: |
          cd ../ros_ws
          source /opt/ros/jazzy/setup.bash
          colcon build